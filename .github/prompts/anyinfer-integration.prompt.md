---
mode: agent
description: Add or change AnyInfer inference code in an application, following the library's own integration procedure.
---

Read and execute `docs/agents/INTEGRATION.md` in full. It is the only authoritative
integration procedure. Start with its Step 0 — `anyinfer agents-md` prints the call shape
for the installed version — and finish with its Step 6 checklist before reporting the work
done. Focus the change here when supplied; otherwise follow the procedure's default scope:

${input:scope:Optional file or feature to focus on, for example "src/app/summarize.py"}
